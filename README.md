# Simple script to move images in folder to hierarchy folders by date

Don't forget to change from and to paths from index.ts
Requirements: NodeJS

To install run: `npm install`
To start simply run `npm start`